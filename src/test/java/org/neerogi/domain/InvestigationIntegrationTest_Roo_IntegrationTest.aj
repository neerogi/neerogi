// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.neerogi.domain;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.neerogi.domain.Investigation;
import org.neerogi.domain.InvestigationDataOnDemand;
import org.neerogi.domain.InvestigationIntegrationTest;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect InvestigationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: InvestigationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: InvestigationIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext.xml");
    
    declare @type: InvestigationIntegrationTest: @Transactional;
    
    @Autowired
    InvestigationDataOnDemand InvestigationIntegrationTest.dod;
    
    @Test
    public void InvestigationIntegrationTest.testCountInvestigations() {
        Assert.assertNotNull("Data on demand for 'Investigation' failed to initialize correctly", dod.getRandomInvestigation());
        long count = Investigation.countInvestigations();
        Assert.assertTrue("Counter for 'Investigation' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void InvestigationIntegrationTest.testFindInvestigation() {
        Investigation obj = dod.getRandomInvestigation();
        Assert.assertNotNull("Data on demand for 'Investigation' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Investigation' failed to provide an identifier", id);
        obj = Investigation.findInvestigation(id);
        Assert.assertNotNull("Find method for 'Investigation' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Investigation' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void InvestigationIntegrationTest.testFindAllInvestigations() {
        Assert.assertNotNull("Data on demand for 'Investigation' failed to initialize correctly", dod.getRandomInvestigation());
        long count = Investigation.countInvestigations();
        Assert.assertTrue("Too expensive to perform a find all test for 'Investigation', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Investigation> result = Investigation.findAllInvestigations();
        Assert.assertNotNull("Find all method for 'Investigation' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Investigation' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void InvestigationIntegrationTest.testFindInvestigationEntries() {
        Assert.assertNotNull("Data on demand for 'Investigation' failed to initialize correctly", dod.getRandomInvestigation());
        long count = Investigation.countInvestigations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Investigation> result = Investigation.findInvestigationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Investigation' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Investigation' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void InvestigationIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Investigation' failed to initialize correctly", dod.getRandomInvestigation());
        Investigation obj = dod.getNewTransientInvestigation(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Investigation' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Investigation' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Investigation' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void InvestigationIntegrationTest.testRemove() {
        Investigation obj = dod.getRandomInvestigation();
        Assert.assertNotNull("Data on demand for 'Investigation' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Investigation' failed to provide an identifier", id);
        obj = Investigation.findInvestigation(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Investigation' with identifier '" + id + "'", Investigation.findInvestigation(id));
    }
    
}
